diff --git a/Services/gemini.py b/Services/gemini.py
index 4a69865..0e132d6 100644
--- a/Services/gemini.py
+++ b/Services/gemini.py
@@ -38,6 +38,17 @@ GEMINI_API_URL = "https://generativelanguage.googleapis.com/v1beta/interactions"
 TIMEOUT_SECONDS = int(os.getenv("GEMINI_TIMEOUT", "25"))
 MAX_CANDIDATES = 20
 
+# A second model to try when the first is overloaded. Google returns 503
+# "experiencing high demand" on popular models at busy times - it says nothing
+# about your code, and a different model is usually free.
+GEMINI_FALLBACK_MODEL = os.getenv("GEMINI_FALLBACK_MODEL", "gemini-3.6-flash")
+
+# Statuses worth trying again: the server is busy or briefly broken, not the
+# request. 401 (bad key) and 400 (bad request) are NOT here - retrying those
+# just fails twice as slowly.
+RETRYABLE_STATUSES = {429, 500, 502, 503, 504}
+RETRY_DELAY_SECONDS = 1.5
+
 
 def is_enabled() -> bool:
     return bool(GEMINI_API_KEY)
@@ -129,22 +140,56 @@ def rerank(query_title: str, query_description: Optional[str], candidates: List[
     }
 
     started = time.monotonic()
-    try:
-        response = requests.post(GEMINI_API_URL, json=payload, headers=headers, timeout=TIMEOUT_SECONDS)
-    except requests.exceptions.Timeout:
-        print(
-            f"[GEMINI] timed out after {TIMEOUT_SECONDS}s with {len(candidates)} candidates "
-            f"- falling back to TF-IDF. Raise GEMINI_TIMEOUT in .env if this repeats."
-        )
-        return None
-    except requests.exceptions.RequestException as e:
-        print(f"[GEMINI] could not reach the API: {e}")
-        return None
+    response = None
+
+    # Try the main model twice, then the fallback model twice. A 503 comes back
+    # in well under a second, so retrying costs almost nothing - unlike a
+    # timeout, which is already slow and is never retried.
+    models = [GEMINI_MODEL] + ([GEMINI_FALLBACK_MODEL] if GEMINI_FALLBACK_MODEL != GEMINI_MODEL else [])
+
+    for model in models:
+        payload["model"] = model
+        for attempt in (1, 2):
+            try:
+                response = requests.post(
+                    GEMINI_API_URL, json=payload, headers=headers, timeout=TIMEOUT_SECONDS
+                )
+            except requests.exceptions.Timeout:
+                print(
+                    f"[GEMINI] {model} timed out after {TIMEOUT_SECONDS}s with "
+                    f"{len(candidates)} candidates - falling back to TF-IDF. "
+                    f"Raise GEMINI_TIMEOUT in .env if this repeats."
+                )
+                return None
+            except requests.exceptions.RequestException as e:
+                print(f"[GEMINI] could not reach the API: {e}")
+                return None
+
+            if response.status_code not in RETRYABLE_STATUSES:
+                break   # success, or a failure that retrying cannot fix
+
+            print(f"[GEMINI] {model} returned {response.status_code} (attempt {attempt}/2, busy)")
+            if attempt == 1:
+                time.sleep(RETRY_DELAY_SECONDS)
+
+        if response is not None and response.status_code not in RETRYABLE_STATUSES:
+            break
+        if len(models) > 1 and model == models[0]:
+            print(f"[GEMINI] {GEMINI_MODEL} unavailable, trying {GEMINI_FALLBACK_MODEL}")
 
     elapsed = time.monotonic() - started
 
+    if response is None:
+        return None
+
     if response.status_code >= 400:
-        print(f"[GEMINI] rejected {response.status_code}: {response.text[:300]}")
+        if response.status_code in RETRYABLE_STATUSES:
+            print(
+                f"[GEMINI] every model was busy ({response.status_code}) after {elapsed:.1f}s "
+                f"- showing TF-IDF results instead. This is Google's side, not yours."
+            )
+        else:
+            print(f"[GEMINI] rejected {response.status_code}: {response.text[:300]}")
         return None
 
     try:
