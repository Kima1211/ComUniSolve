diff --git a/Services/gemini.py b/Services/gemini.py
index 27b6237..487e122 100644
--- a/Services/gemini.py
+++ b/Services/gemini.py
@@ -20,6 +20,7 @@ identifying is included; no names, no emails, no user ids.
 """
 import json
 import os
+import time
 from typing import List, Optional
 
 import requests
@@ -31,7 +32,10 @@ GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")          # optional by design
 GEMINI_MODEL = os.getenv("GEMINI_MODEL", "gemini-3.8-flash")
 GEMINI_API_URL = "https://generativelanguage.googleapis.com/v1beta/interactions"
 
-TIMEOUT_SECONDS = 8
+# 8 seconds was too tight in practice: a re-rank of 20 candidates has real
+# generation work to do, and the first call after an idle period is slower
+# still. Tunable from .env without touching code.
+TIMEOUT_SECONDS = int(os.getenv("GEMINI_TIMEOUT", "25"))
 MAX_CANDIDATES = 20
 
 
@@ -48,7 +52,7 @@ _RESPONSE_SCHEMA = {
                 "type": "object",
                 "properties": {
                     "id": {"type": "integer"},
-                    "relevance": {"type": "string", "enum": ["high", "medium", "low", "none"]},
+                    "relevance": {"type": "string", "enum": ["high", "medium", "low"]},
                     "reason": {"type": "string"},
                 },
                 "required": ["id", "relevance", "reason"],
@@ -72,10 +76,11 @@ def _build_prompt(query_title: str, query_description: Optional[str], candidates
         "all describe the same problem).\n\n"
         f"NEW PROBLEM\nTitle: {query_title}\nDescription: {query_description or '(none)'}\n\n"
         f"EXISTING PROBLEMS\n{listing}\n\n"
-        "For each existing problem, judge how likely it describes the same underlying "
-        "issue as the new one. Use relevance 'high', 'medium', 'low' or 'none'. Give a "
-        "reason of at most 20 words, written for the person who posted, explaining the "
-        "connection. Only use ids from the list above. Return every id you were given."
+        "Return ONLY the existing problems that describe the same underlying issue as the "
+        "new one. Leave unrelated problems out of your answer entirely - do not list them. "
+        "For each one you return, give relevance 'high', 'medium' or 'low', and a reason of "
+        "at most 15 words written for the person who posted. Only use ids from the list "
+        "above. If none are related, return an empty list."
     )
 
 
@@ -105,12 +110,21 @@ def rerank(query_title: str, query_description: Optional[str], candidates: List[
         "x-goog-api-key": GEMINI_API_KEY,
     }
 
+    started = time.monotonic()
     try:
         response = requests.post(GEMINI_API_URL, json=payload, headers=headers, timeout=TIMEOUT_SECONDS)
+    except requests.exceptions.Timeout:
+        print(
+            f"[GEMINI] timed out after {TIMEOUT_SECONDS}s with {len(candidates)} candidates "
+            f"- falling back to TF-IDF. Raise GEMINI_TIMEOUT in .env if this repeats."
+        )
+        return None
     except requests.exceptions.RequestException as e:
         print(f"[GEMINI] could not reach the API: {e}")
         return None
 
+    elapsed = time.monotonic() - started
+
     if response.status_code >= 400:
         print(f"[GEMINI] rejected {response.status_code}: {response.text[:300]}")
         return None
@@ -140,14 +154,17 @@ def rerank(query_title: str, query_description: Optional[str], candidates: List[
             pid = m.get("id")
             # Only ids we actually sent. The model's reply is data, and data
             # from outside is never trusted to be well-behaved.
-            if pid in valid_ids and m.get("relevance") in {"high", "medium", "low", "none"}:
+            if pid in valid_ids and m.get("relevance") in {"high", "medium", "low"}:
                 ranked[pid] = {
                     "relevance": m["relevance"],
                     "reason": str(m.get("reason", ""))[:300],
                 }
 
-        print(f"[GEMINI] re-ranked {len(ranked)} of {len(candidates)} candidates")
-        return ranked or None
+        print(f"[GEMINI] {len(ranked)} related of {len(candidates)} candidates in {elapsed:.1f}s")
+        # An empty dict is a real answer - "the AI looked and found nothing
+        # related". Only None means the AI could not be consulted at all. The
+        # caller needs to tell those apart.
+        return ranked
 
     except (ValueError, KeyError, TypeError) as e:
         print(f"[GEMINI] could not parse the response: {e}")
diff --git a/frontend/src/components/PostProblem.jsx b/frontend/src/components/PostProblem.jsx
index 24cd5ea..693462e 100644
--- a/frontend/src/components/PostProblem.jsx
+++ b/frontend/src/components/PostProblem.jsx
@@ -135,6 +135,13 @@ function PostProblem() {
                         hint="Someone may already have asked this. Check before posting — an answer might be waiting."
                     />
 
+                    {aiUsed && matches.length === 0 && (
+                        <p className="rounded-xl border border-slate-200 bg-white p-4 text-sm text-slate-600">
+                            The AI checked every existing problem and found none related to
+                            yours. Go ahead and post it.
+                        </p>
+                    )}
+
                     {title.trim().length >= 6 && !aiUsed && (
                         <button
                             type="button"
diff --git a/routers/matching.py b/routers/matching.py
index 1394db1..2089d00 100644
--- a/routers/matching.py
+++ b/routers/matching.py
@@ -70,11 +70,13 @@ def _build_matches(
         pool = [c for c in candidates if c["id"] in tfidf_scores]
         ai_ranked = gemini.rerank(query_title, query_description, pool)
 
-    if ai_ranked:
+    # `None` means the AI could not be consulted (no key, timeout, quota).
+    # An empty dict means it WAS consulted and found nothing related - a real
+    # answer, and not a reason to fall back to weaker word matching.
+    if ai_ranked is not None:
         chosen = [
             (pid, tfidf_scores.get(pid, 0.0), info)
             for pid, info in ai_ranked.items()
-            if info["relevance"] != "none"
         ]
         # Gemini's judgement leads; the TF-IDF score breaks ties.
         chosen.sort(key=lambda t: (_RELEVANCE_ORDER.get(t[2]["relevance"], 0), t[1]), reverse=True)
@@ -106,7 +108,7 @@ def _build_matches(
                 reason=info["reason"] if info else None,
             )
         )
-    return results, bool(ai_ranked)
+    return results, ai_ranked is not None
 
 
 @router.post("/problems/match", response_model=MatchResponse)
